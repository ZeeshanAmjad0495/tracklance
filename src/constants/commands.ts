enum Commands {
  Add = 'add',
  Delete = 'delete',
  List = 'list',
  ListDone = 'list done',
  ListInProgress = 'list in-progress',
  ListTodo = 'list todo',
  MarkDone = 'mark-done',
  MarkInProgress = 'mark-in-progress',
  Update = 'update',
}

export default Commands;
